# Agent entry point

Read and follow `.agents/AGENTS.md` as the central project instructions.

Use only the skills under `.agents/skills/` that are relevant to the current task; do not duplicate their rules here.

For delegated implementation with OpenCode, follow the routing and workflow defined by `.agents/AGENTS.md` and the relevant executor skill.
